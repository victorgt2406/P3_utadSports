import express from "express";
const router = express.Router();
// get all
router.get("/",);
// get one
router.get("/:id",);
// insert one
router.post("/",);
// put one
router.put("/:id",);
// delete one
router.delete("/:id",);

export default router;