import express from "express";
const router = express.Router();
// create a message
router.post('/',);

// get user messages
router.get('/',);

// get a message
router.get('/:id',);

// delete a message
router.delete('/:id',);

// module.exports = router;
export default router;